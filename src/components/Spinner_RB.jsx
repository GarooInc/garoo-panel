import { Spinner } from "react-bootstrap";

const Spinner_RB = ({ animation, variant, size }) => {
    return (
        <Spinner animation={animation} variant={variant} size={size} />
    );
};

export default Spinner_RB;


